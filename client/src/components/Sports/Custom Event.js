import AddEventForm from "../CustomEvent";

const CustomEvent = () => {
	return (
		<>
			<h1>mySPORTScal</h1>
			<h2>Create your own Event</h2>
			<AddEventForm/>
		</>
	);
};

export default CustomEvent;
